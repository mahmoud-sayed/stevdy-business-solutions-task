import { createTheme } from "@mui/material";

const theme = createTheme({
  colors: {
    white: '#fff',
    deepOrange: '#FF6057',
    textColor: '#3B3A3A',
    textColor2: '#8F8F8F',
    startColor: '#CB48F9'
  }
});
export default theme;